[project]
name = "finscope"
version = "0.1.0"
description = "A personal FP&A platform: variance analysis, forecasting, Monte Carlo planning."
requires-python = ">=3.10"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
